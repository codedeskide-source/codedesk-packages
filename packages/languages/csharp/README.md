# C# Language Support

Optional C# console and Windows GUI templates and .NET SDK compiler
integration for CodeDesk. Windows GUI projects use WinExe and launch the
published .exe directly so they do not create an unwanted command window.
